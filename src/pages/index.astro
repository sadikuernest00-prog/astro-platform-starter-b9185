---
const FORM_ENDPOINT = "https://formspree.io/f/xzznjkvd";
const CEO_NAME = "Robert Amicolsen";
const CEO_EMAIL = "robertamicolsen@gmail.com";
---

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Amicbridge — Verified Borrow & Lend Network</title>
  <meta
    name="description"
    content="Amicbridge is a verified network bridge connecting borrowers and lenders. Manual ID verification by the CEO."
  />
</head>

<body>
  <!-- HEADER -->
  <header class="header">
    <div class="container header-inner">
      <div class="brand">
        <strong>Amicbridge</strong>
        <span class="muted">Verified lending network</span>
      </div>

      <nav class="nav">
        <a href="#how">How it works</a>
        <a href="#verification">Verification</a>
        <a href="#request">Request</a>
        <a href="#contact">Contact</a>
      </nav>
    </div>
  </header>

  <main>
    <!-- HERO -->
    <section class="section hero">
      <div class="container">
        <h1>A verified network for borrowing and lending</h1>

        <p class="lead">
          Amicbridge is a network bridge for serious people and business partners who want to borrow money,
          lend money, or request general investment strategy guidance.
        </p>

        <div class="actions">
          <a class="btn primary" href="#request">Send a request</a>
          <a class="btn" href="#verification">Manual verification</a>
        </div>
      </div>
    </section>

    <!-- HOW IT WORKS -->
    <section class="section" id="how">
      <div class="container">
        <h2>How it works</h2>

        <ol class="steps">
          <li>
            <strong>Submit a request</strong>
            <span>You explain what you need: borrowing, lending, or investment strategy.</span>
          </li>
          <li>
            <strong>Review for clarity</strong>
            <span>Each request is reviewed manually for seriousness and completeness.</span>
          </li>
          <li>
            <strong>Manual ID verification</strong>
            <span>Identity is verified manually before access to the network.</span>
          </li>
          <li>
            <strong>Direct agreement</strong>
            <span>Borrowers and lenders communicate directly and agree on terms.</span>
          </li>
        </ol>
      </div>
    </section>

    <!-- VERIFICATION -->
    <section class="section alt" id="verification">
      <div class="container">
        <h2>Manual ID verification</h2>

        <p class="muted">
          To reduce fraud and improve trust, Amicbridge requires manual identity verification
          before users are granted access to the network.
        </p>

        <div class="box">
          <ul class="clean-list">
            <li>Identity is reviewed manually by the CEO</li>
            <li>Basic document consistency and authenticity checks</li>
            <li>Email and contact confirmation</li>
            <li>No automated systems or third-party profiling</li>
          </ul>

          <p class="muted">
            Verification improves trust but does not guarantee repayment or remove financial risk.
            All parties must perform their own due diligence.
          </p>
        </div>
      </div>
    </section>

    <!-- REQUEST FORM -->
    <section class="section" id="request">
      <div class="container">
        <h2>Send a request</h2>

        <div class="box">
          <form action={FORM_ENDPOINT} method="POST" class="form">
            <input type="hidden" name="source" value="amicbridge.com" />

            <div class="grid2">
              <label>
                Full name / Company
                <input name="name" required />
              </label>

              <label>
                Email
                <input type="email" name="email" required />
              </label>
            </div>

            <label>
              Request type
              <select name="requestType" id="requestType" required>
                <option value="" disabled selected>Select</option>
                <option value="Borrow request">Borrow request</option>
                <option value="Lending request">Lending request</option>
                <option value="Investment strategy request">Investment strategy request</option>
              </select>
            </label>

            <div id="borrowFields" hidden>
              <label>Amount (NOK)
                <input name="borrow_amount" />
              </label>
              <label>Repayment plan
                <textarea name="borrow_plan" rows="4"></textarea>
              </label>
            </div>

            <div id="lendFields" hidden>
              <label>Amount available (NOK)
                <input name="lend_amount" />
              </label>
              <label>Conditions
                <textarea name="lend_conditions" rows="4"></textarea>
              </label>
            </div>

            <div id="investFields" hidden>
              <label>Investment goal
                <textarea name="invest_goal" rows="4"></textarea>
              </label>
            </div>

            <label>
              Additional details
              <textarea name="message" rows="6" required></textarea>
            </label>

            <label class="checkbox">
              <input type="checkbox" required />
              <span>
                I understand identity is verified manually and agreements are made directly between parties.
              </span>
            </label>

            <button class="btn primary" type="submit">Send request</button>
          </form>
        </div>
      </div>
    </section>

    <!-- CONTACT -->
    <section class="section alt" id="contact">
      <div class="container">
        <h2>Contact</h2>

        <div class="contact-box">
          <strong>{CEO_NAME}</strong>
          <div class="muted">Founder & CEO — Amicbridge</div>
          <p>Email: <a href={`mailto:${CEO_EMAIL}`}>{CEO_EMAIL}</a></p>
        </div>
      </div>
    </section>

    <!-- FOOTER -->
    <footer class="footer">
      <div class="container footer-inner">
        <div>
          <strong>Amicbridge</strong>
          <div class="muted">Personally verified network</div>
        </div>

        <div class="muted">
          © {new Date().getFullYear()} Amicbridge
        </div>
      </div>
    </footer>
  </main>

  <script>
    const type = document.getElementById("requestType");
    const borrow = document.getElementById("borrowFields");
    const lend = document.getElementById("lendFields");
    const invest = document.getElementById("investFields");

    type.addEventListener("change", () => {
      borrow.hidden = type.value !== "Borrow request";
      lend.hidden = type.value !== "Lending request";
      invest.hidden = type.value !== "Investment strategy request";
    });
  </script>

  <style>
    body {
      margin: 0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial;
      background: #fff;
      color: #111827;
    }
    .container { max-width: 900px; margin: auto; padding: 0 1rem; }
    .header { border-bottom: 1px solid #e5e7eb; }
    .header-inner { display: flex; justify-content: space-between; padding: 1rem 0; }
    .nav a { margin-left: 1rem; text-decoration: none; color: #4b5563; font-weight: 600; }
    .section { padding: 2.2rem 0; }
    .section.alt { background: #f9fafb; }
    .box { border: 1px solid #e5e7eb; padding: 1rem; border-radius: 8px; background: #fff; }
    label { display: block; margin-top: .8rem; font-weight: 600; }
    input, textarea, select {
      width: 100%;
      padding: .6rem;
      border-radius: 6px;
      border: 1px solid #e5e7eb;
    }
    .grid2 { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
    .btn {
      margin-top: 1rem;
      padding: .6rem 1rem;
      border: 1px solid #111827;
      background: #111827;
      color: #fff;
      border-radius: 6px;
      font-weight: 700;
    }
    .checkbox { display: flex; gap: .5rem; margin-top: 1rem; color: #4b5563; }
    .footer { border-top: 1px solid #e5e7eb; padding: 1.4rem 0; }
    .footer-inner { display: flex; justify-content: space-between; color: #4b5563; }
    .muted { color: #4b5563; }
    @media(max-width:720px){ .nav{display:none} .grid2{grid-template-columns:1fr} }
  </style>
</body>
</html>
